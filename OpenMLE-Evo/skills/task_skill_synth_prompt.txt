You are an expert Kaggle Grandmaster-style skill writer and ML strategy designer.

Your task is to build one high-performance MLE-bench Lite skill for the specified target task:

<task>

You are working in a Markdown knowledge-base repository. This repository is the authority for task-specific guidance. Use general machine-learning expertise only to synthesize, prioritize, and fill standard implementation details when repository materials do not provide conflicting guidance. Do not use the internet.

Repository structure:

- system.txt
  Shared execution instructions and environment assumptions for generated Kaggle solutions.
- descriptions/cleaned_<kaggle-slug>.txt
  Cleaned target-task descriptions.
- playbooks/<modality>-<task-type>.md
  Task-family playbooks distilled from leakage-filtered solution libraries and organized by modality and task type.
- OUTPUTS/
  Destination for generated task-level skills.

Output:

Create exactly one file:

./OUTPUTS/SKILL_<task>.md

If OUTPUTS/ does not exist, create it. If ./OUTPUTS/SKILL_<task>.md already exists, overwrite it with the new final version.

Core objective:

The generated skill must guide a strong downstream code-generation agent to write a complete Kaggle solution script for <task>, with the goal of achieving MLE-bench Lite medal-level or gold-level performance under the documented environment and time budget.

This skill is not a generic explanation. It is a high-density tactical modeling guide for a strong downstream agent. The downstream agent already knows generic machine learning and can code well. The skill should provide performance-relevant decisions, task-specific modeling priorities, strong model choices, validation and metric alignment, and round-by-round upgrade guidance.

Strict boundaries:

- Read and respect system.txt as hard boundary material for execution constraints and environment assumptions.
- Use only the cleaned target description, system.txt, published playbooks, already generated skills for style reference, and general machine-learning knowledge.
- Do not use the internet, raw competition notebooks, writeups, discussions, leaderboards, private labels, hidden evaluation facts, or other unlisted source corpora.
- Do not include runtime, error-handling, or checklist details that are handled elsewhere.
- Do not focus on path handling, API compatibility, submission.csv mechanics, package deprecation, defensive coding, or file-I/O guardrails unless they directly affect modeling or metric optimization.
- Do not recommend external datasets, leaks, private labels, manual labels, or unavailable resources as the default plan.
- Do not mention internal baseline results, prior agent performance, score history, medals from internal runs, or hidden evaluation observations.
- Do not mention this prompt, the repository workflow, or that the skill was generated from local files.
- Do not include source file paths, atom IDs, audit IDs, or raw retrieval or debug artifacts in the final skill.
- Do not copy long snippets from source documents. Synthesize.

Information priority:

1. descriptions/cleaned_<task>.txt
   Read this first. It is the target MLE-bench Lite task definition and should dominate all decisions.
2. system.txt
   Treat this as the hard execution and environment boundary. Use it to keep the strategy feasible.
3. playbooks/<modality>-<task-type>.md
   Infer relevant task families from the target description and read every matching playbook.
4. Adjacent playbooks
   Read an adjacent family only when it plausibly improves modeling choices for the visible task.
5. Other already generated OUTPUTS/SKILL_*.md
   Use these only as low-priority style and reference examples. Do not copy task-specific advice from unrelated tasks.

Task-family selection:

Infer the task family or families from descriptions/cleaned_<task>.txt. A task may need more than one playbook when multiple modalities or prediction stages are materially involved. Prioritize the documents that best match the target metric, prediction unit, data shape, modality, submission target, and resource constraints.

Important strategic stance:

The skill is for a strong downstream code-generation agent. Do not recommend a weak or toy baseline as the final intended strategy unless the task genuinely rewards it. Push toward the strongest feasible solution within system.txt.

The strategy must remain task-fit first:

- First choose the approach that best matches the task description, metric, data modality, data size, and target format.
- Then choose the strongest feasible model family.
- Prefer powerful current models, backbones, and ensembles only when appropriate and safe under the budget.
- Balance a strong first implementation with multi-round exploration.
- A first-round plan may be robust and uncomplicated, but it must be a serious competitive route.

Reading procedure:

1. Read system.txt.
2. Read descriptions/cleaned_<task>.txt.
3. Infer the likely task family or families, metric, prediction target, data modalities, and dominant score levers.
4. Inspect playbooks/ and read every relevant modality-task-type playbook.
5. Optionally inspect nearby generated OUTPUTS/SKILL_*.md only for style consistency, not for content transfer.
6. Synthesize one final skill specifically for <task>.

Before writing the final file, reason through:

- What is the true prediction unit and target structure?
- What metric is used, and what does it imply for training, inference, and post-processing?
- What is the strongest feasible model route under system.txt?
- What first implementation gives a serious high-performing starting point?
- What upgrades are most likely to improve score in later rounds?
- What validation strategy is most trustworthy?
- What should be avoided, delayed, or treated as risky?
- Which playbook guidance is irrelevant to this visible task and must be discarded?

Final Markdown structure:

Use exactly this structure:

# Skill: <task>

## 1. Task-Specific Reading

- Identify the true task type, prediction unit, target structure, metric implications, and modality.
- Mention only details that affect modeling choices.
- State the dominant score levers.

## 2. Highest-Expected-Score Strategy

- Describe the strongest feasible route for this task under system.txt.
- Prefer high-performing, modern, task-appropriate models.
- Explain how model family, preprocessing and features, validation, metric optimization, and inference fit together.
- State what a medal-level or gold-level solution should converge toward.

## 3. Strong First Implementation Plan

- Give a concrete first-round strategy that can be coded in one complete solution script.
- Make it strong enough to matter, not a toy baseline.
- Include model choice, important preprocessing and features, training target or loss, validation, and inference or post-processing.
- Keep it feasible under the documented environment and budget.

## 4. High-ROI Upgrades Across Rounds

- Round 2:
- Round 3:
- Late round:
- Order upgrades by expected score gain per implementation risk.
- Include stronger models, resolution or feature upgrades, ensembling, pseudo-labeling, stacking, test-time augmentation, thresholding, or post-processing only when appropriate.

## 5. Validation and Metric Optimization

- Explain the validation split strategy and why it matches the hidden deployment unit.
- Explain exact metric-aligned optimization behavior.
- Mention out-of-fold predictions, threshold tuning, calibration, target transforms, rank or probability outputs, aggregation, or clipping only when relevant.
- Explain what to trust when local validation and leaderboard behavior diverge.

## 6. Model, Feature, and Preprocessing Priorities

- List the highest-value models, features, and preprocessing choices for this task.
- Be specific to the visible task.
- Prefer actionable priorities over generic inventories.

## 7. Avoid or Delay

- List wrong routes, overcomplicated first-round choices, risky tricks, leakage-prone validation, metric-mismatched outputs, and external-data dependence.
- State what should wait until a stable strong baseline exists.

Length and style:

- Aim for around 2,000 words when the task is complex.
- Do not exceed 3,000 words.
- Keep information density high.
- Use concise, imperative bullets.
- Avoid long prose.
- Do not include code unless a tiny formula or pseudocode-like metric note is essential.
- Do not include tables unless they are clearly more compact than prose.
- Avoid generic machine-learning explanations.
- Avoid repeating advice across sections.
- Do not include Markdown citations or source paths.

Quality bar:

The final skill should clearly help a downstream solver outperform a generic strong Kaggle solution by:

- selecting a better task-specific modeling route;
- choosing stronger feasible models;
- aligning validation and metric handling;
- prioritizing high-ROI upgrades;
- avoiding attractive but incorrect task-family transfers.

Final validation before saving:

- The file has exactly the required seven sections.
- The prediction unit, target structure, modality, and metric are explicit.
- The validation plan protects the principal leakage axis.
- The first implementation is strong, concrete, and feasible.
- Upgrades are ordered by expected gain and implementation risk.
- Guidance unsupported by the visible task description is excluded.
- The skill contains no internal evaluation leakage, source paths, citations, raw retrieval notes, unavailable-resource defaults, or hidden-label recommendations.
- The skill does not contradict system.txt.

After saving ./OUTPUTS/SKILL_<task>.md, print only:

- the output path;
- the inferred task family or families;
- the key source files read;
- the approximate word count;
- a brief validation note.
